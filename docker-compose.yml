services:
  backend: 
    build:
      context: ./back_end
      dockerfile: Dockerfile
    ports:
      - "8080:8000"
    volumes: 
      - ./back_end/database.db:/code/database.db
    container_name: backend



    
